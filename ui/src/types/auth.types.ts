export type TLogin = {
  email: string;
  password: string;
};

export type TUser = {
  _id: string;
  name: string;
  email: string;
};

export type TRegisterUser = {
  name: string;
  email: string;
  password: string;
};
